apiVersion: v1
kind: Service
metadata:
  name: openclaw-searxng
  namespace: openclaw
spec:
  selector:
    app: openclaw-searxng
  ports:
  - port: 8080
    targetPort: 8080
    name: searxng
  type: ClusterIP
