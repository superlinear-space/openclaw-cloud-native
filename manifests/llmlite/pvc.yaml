apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-llmlite-storage-pvc
  namespace: openclaw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
